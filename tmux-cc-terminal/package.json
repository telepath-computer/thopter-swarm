{
  "name": "xtermjs-tmux-cc",
  "version": "0.1.0",
  "description": "POC: Electron + xterm.js + tmux Control Center mode",
  "main": "main.js",
  "scripts": {
    "start": "xvfb-run -a electron .",
    "test:e2e": "xvfb-run -a npx playwright test",
    "test": "npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.0",
    "electron": "^40.0.0"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/xterm": "^5.5.0"
  }
}
