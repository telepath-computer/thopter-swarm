npm install
get a runloop api key. set in your environment by default, or at least, in .env.local as RUNLOOP_API_KEY=xxx
get an upstash redis instance setup for thopter status reporting. get the url (redis://default:xxx@yyy.upstash.io:6379) and also put it in .env.local  as REDIS_URL=xxx



